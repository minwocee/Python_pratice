# https://www.acmicpc.net/problem/1972


def check(text):
    text_len = len(text)
    
    if text_len ==1 or :
        return print(text, "is surprising.")
    
    for i in range(0,text_len-1):
        
        mlist =[]
        for k in range(text_len-(i+1)):
            
    

import sys
while(text!='*'):
    text = sys.stdin.readline().strip()
    def check(text)